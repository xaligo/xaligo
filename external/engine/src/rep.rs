//! Reserved external-representation layer.
//!
//! The current engine returns calculated data through `ctl` and therefore has
//! no repository implementation. Future Rust-owned encoders, such as a PPTX
//! package writer, may be added here without moving calculation out of `usc`.
