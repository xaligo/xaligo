# Build artifacts are placed here by `make build-wasm`.
# These files are intentionally excluded from git (see ../../.gitignore).
# wasm/xaligo.wasm — TS/PptxGenJS exporter compiled for Go's WASI adapter.
