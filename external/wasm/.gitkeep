# Build artifacts — placed here by `make build-wasm`.
# These files are intentionally excluded from git (see ../.gitignore).
# wasm/xaligo.wasm   — compiled Go WASM binary  (~90 MB, embed all SVGs)
# wasm/wasm_exec.js  — Go runtime glue (from $(go env GOROOT)/lib/wasm/)
